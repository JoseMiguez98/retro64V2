{
  "name": "@retro64/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "End-to-end verification suite. Boots the real signaling server + web dev server and drives them with real browsers (AGENTS.md §1.4).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "playwright show-report",
    "install:browsers": "playwright install chromium firefox",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2",
    "socket.io-client": "^4.8.1",
    "typescript": "^5.7.2"
  }
}
